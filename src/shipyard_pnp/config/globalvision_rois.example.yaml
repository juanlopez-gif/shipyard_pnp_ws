# Example ROI grid configuration for globalvision calibrator
# Copy from old globalvision_calibrator.py output format
# See ARCHITECTURE_PLAN.md C-VENDOR-GLOBALVISION-4

grid:
  cols: 4
  rows: 3
  slots:
    - {id: "s1.1", x: 100, y: 80, w: 60, h: 60}
    # TODO: fill from actual calibration run
